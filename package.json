{
  "name": "chatapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npm run dev --prefix services/server",
    "web": "npm run dev --prefix app/web",
    "desktop": "npm run dev --prefix app/desktop",
    "dev": "concurrently -n server,web -c blue,green \"npm run server\" \"npm run web\""
  }
}
